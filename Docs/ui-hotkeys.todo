✔ [Phase 1 - Discovery and Contracts] Define dynamic bottom-panel targeting and DF binding resolution contracts. @done(26-07-31 12:00)
    ✔ Confirm target focus and overlay lifecycle for fortress mode (`dwarfmode/Default`). @done(26-07-31 12:00)
    ✔ Identify all bottom-panel ui-menu buttons that need labels (citizen, etc.) and define one stable semantic ID per button. @done(26-07-31 12:00)
    ✔ Design a dynamic bounds sampler that re-queries button rectangles from native UI tiles and invalidates cache on size/layout changes. @done(26-07-31 12:00)
    ✔ Design a binding resolver that reads the active keybinding from DF for each semantic button ID. @done(26-07-31 12:00)
    ✔ Define failure behavior: if bounds or binding are unavailable for one button, suppress only that button label. @done(26-07-31 12:00)
    Decisions:
    - Focus/lifecycle contract: render only while `dwarfmode/Default` is active; clear all cached geometry and labels immediately when focus leaves the screen.
    - Semantic-ID contract: each label target is keyed by a stable semantic menu ID (for example `citizens`) that is independent of on-screen position and current hotkey text.
    - Button-catalog contract: the model owns the complete bottom-panel button catalog and maps each semantic ID to one DF action binding key; overlay code consumes snapshots only.
    - Geometry contract: bounds are dynamically sampled from live native tiles, cached per layout signature, and invalidated on screen-size changes or panel-layout shifts.
    - Binding contract: hotkey text is resolved from active DF bindings every refresh cycle from the semantic ID's mapped action; no hardcoded label fallback is permitted.
    - Failure contract: labels are suppressed per button when either bounds or active binding data is unavailable; other button labels remain visible.
    - Rendering contract: each label anchor is derived from the sampled bounding box (corner + inset), not fixed coordinates.
    Docs:
    - README.md (module, overlay, and reload conventions)
    - Docs/hover-action-rail-proposal.md (target/anchor ownership patterns and dynamic placement ideas)
    - https://docs.dfhack.org/en/stable/docs/dev/Lua%20API.html (DFHack Lua API reference)

✔ [Phase 2 - Model Module] Implement dynamic button bounds + active hotkey model. @done(26-07-31 12:15)
    ✔ Add a new `dwarfui/` model module for: @done(26-07-31 12:15)
        - querying current bottom-panel button bounds,
        - mapping bounds to semantic button IDs,
        - resolving active DF keybinding labels,
        - exposing one snapshot API consumed by overlay rendering.
    ✔ Implement cache + invalidation keyed by screen dimensions and layout signatures. @done(26-07-31 12:15)
    ✔ Normalize returned label text (single visible hotkey token for corner label rendering). @done(26-07-31 12:15)
    ✔ Add LuaDoc comments for all public classes/methods. @done(26-07-31 12:15)
    Deliverables:
    - Implemented `UiHotkeyModel` in `src/scripts_modinstalled/dwarfui/ui_hotkeys.lua`.
    - Added immutable menu enum, catalog contracts, dynamic bounds sampling context, layout-signature cache invalidation, and normalized snapshot output.
    - Added default DF/DFHack binding lookup hooks with injectable resolver override.
    - Added unit coverage in `tests/ui_hotkeys.spec.lua` for cache invalidation, per-button suppression, normalization, and inactive-cache reset.
    Docs:
    - src/scripts_modinstalled/dwarfui/minecart_route.lua (dynamic row/bounds model pattern)
    - src/scripts_modinstalled/dwarfui/mood_popover.lua (layout discovery and cache invalidation pattern)

✔ [Phase 3 - Overlay] Render corner hotkey labels on live native button bounds. @done(26-07-31 12:30)
    ✔ Add a fullscreen passive overlay script in `src/scripts_modinstalled/` for bottom-panel hotkey labels. @done(26-07-31 12:30)
    ✔ Keep all input pass-through; never consume clicks or keyboard events. @done(26-07-31 12:30)
    ✔ Draw a small label in the chosen corner inset for each currently visible button from the model snapshot. @done(26-07-31 12:30)
    ✔ Re-sample model data on render/update so labels track slight layout changes across window sizes. @done(26-07-31 12:30)
    ✔ Verify citizen button shows `u` when DF active binding is default. @done(26-07-31 12:30)
    Deliverables:
    - Implemented overlay script `src/scripts_modinstalled/dwarfui-ui-hotkeys.lua` with fullscreen pass-through rendering and model-driven label drawing.
    - Added `UiMenuHotkeysOverlay` snapshot sync on render and on update, with per-button bounds-anchored corner label placement.
    - Added overlay coverage in `tests/ui_hotkeys_overlay.spec.lua`, including a citizen `u` render case and resampling behavior assertions.
    - Verified with Unit Tests task: pass (401 successes, 0 failures).
    Docs:
    - src/scripts_modinstalled/dwarfui-mood-popover.lua (fullscreen overlay structure)
    - src/scripts_modinstalled/dwarfui-minecart-route-markers.lua (native-overlay rendering integration)

✔ [Phase 4 - Runtime Integration] Register and reload safely. @done(26-07-31 12:40)
    ✔ Add any new internal model module(s) to `dwarfui/module_registry.lua` in dependency order. @done(26-07-31 12:40)
    ✔ Add the new overlay registration script to overlay reload/retirement flow in `src/scripts_modinstalled/dwarfui.lua`. @done(26-07-31 12:40)
    ✔ Verify `dwarfui reload` reconstructs this feature without stale state. @done(26-07-31 12:40)
    Deliverables:
    - Added `dwarfui/ui_hotkeys` contract registration to `src/scripts_modinstalled/dwarfui/module_registry.lua`.
    - Added `dwarfui-ui-hotkeys` to overlay retirement/rescan lifecycle in `src/scripts_modinstalled/dwarfui.lua`.
    - Updated reload/retirement integration expectations in `tests/dwarfui_command.spec.lua`.
    - Updated module ordering expectations in `tests/module_registry.spec.lua`.
    - Verified via Unit Tests task: pass (401 successes, 0 failures).
    Docs:
    - src/scripts_modinstalled/dwarfui/module_registry.lua
    - src/scripts_modinstalled/dwarfui.lua
    - README.md (Runtime validation and reload)

☐ [Phase 5 - Unit Tests] Add deterministic coverage for geometry, bindings, and pass-through behavior.
    ☐ Add a model spec covering:
        - multiple window sizes with slight layout shifts,
        - dynamic bounds recomputation,
        - active binding lookup and label normalization,
        - per-button suppression when data is missing.
    ☐ Add an overlay spec covering:
        - default-enabled fullscreen contract,
        - render placement against model-provided bounds,
        - no input consumption/regression of native behavior.
    ☐ Update module registry tests if new module ordering changes.
    ☐ Update package contract tests for any newly shipped module files.
    Docs:
    - tests/mood_popover_overlay.spec.lua
    - tests/minecart_route_overlay.spec.lua
    - tests/module_registry.spec.lua
    - tests/package_contract.spec.lua
    - tests/README.md

☐ [Phase 6 - Verification and Documentation] Validate implementation and close task.
    ☐ Run Build task (`Tools/Build.ps1`).
    ☐ Run Unit Tests task (`Tools/Run-UnitTests.ps1`).
    ☐ Add README note for bottom-panel hotkey labels feature and behavior constraints.
    ☐ Mark project todo entry complete after verification.
    Docs:
    - README.md
    - Docs/project.todo
